<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block w-full outline-none rounded-lg px-5 py-2 bg-zinc-800"
            type="text"
            name="title"
            required
            placeholder="title goes here" />
          <textarea
            class="block w-full outline-none rounded-lg mt-2 px-5 py-2 bg-zinc-800 resize-none"
            name="details"
            required
            placeholder="write about your task"></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
            value="Create task" />
        </form>
      </div>

      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){%>
            <div class="task min-w-72 px-3 py-4 rounded-lg bg-zinc-800">
                <h1 class="text-white text-3xl tracking-tighter">
                    <%=val %>
                </h1>
                <a class="inline-block mt-3 bg-blue-500 py-2 px-2 rounded-md" href="/file/<%=val %>"
                  >Read More</a
                >
                <a class="inline-block mt-3 bg-red-500 py-2 px-2 rounded-md" href="/delete/<%=val %>"
                  >Delete</a
                >
              </div>
            <% }) %> <%
        }else{ %>
        <h3 class="text-zinc-600">No task</h3>
        <% }%>
    
        </div>
       
      </div>
    </div>
  </body>
</html>
